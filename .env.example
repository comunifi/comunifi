# Nostr relay URL (WebSocket)
RELAY_URL=''

# Client signature private key (32-byte Ed25519 seed as hex, 64 characters)
# Used to sign payloads to identify messages from Comunifi clients
# Generate with: openssl rand -hex 32
CLIENT_SIGNATURE_PRIVATE_KEY=''
